{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@chenglou/pretext": "^0.0.5",
    "posthog-js": "^1.407.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^6.30.3",
    "sass": "^1.62.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "og": "node scripts/generate-og.mjs",
    "manual-test-mobile": "npm run dev -- --host",
    "e2e-test:headless": "playwright test",
    "e2e-test:headed": "playwright test --headed",
    "e2e-test:ui": "playwright test --ui",
    "test:all": "npm run e2e-test:headless && npm run e2e-test:headed",
    "deploy:master": "git checkout master && git merge test && git push origin master && open-cli https://jensrot.netlify.app",
    "deploy:test": "git checkout test && git merge master && git push origin test && git checkout master && open-cli https://test--jensrot.netlify.app",
    "deploy:all": "git checkout master && git push origin master && git push origin master:test && open-cli https://jensrot.netlify.app && open-cli https://test--jensrot.netlify.app"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3",
    "@vitejs/plugin-react": "^3.1.0",
    "open-cli": "^9.0.0",
    "typescript": "^5.7.3",
    "vite": "^4.2.2"
  }
}
